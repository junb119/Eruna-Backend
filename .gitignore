# node_modules는 무조건 제외 (용량 큼, 설치로 대체 가능)
node_modules/

# 환경변수 (.env)는 보안 상 절대 올리면 안 됨
.env

# Mac OS 자동 생성 파일
.DS_Store

# 로그파일 및 디버깅 출력
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 테스트 커버리지 결과
coverage/

# IDE 설정 파일 (VSCode 기준)
.vscode/

# OS별 임시 파일
Thumbs.db

*.sql
